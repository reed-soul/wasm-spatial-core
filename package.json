{
  "name": "wasm-spatial-core-dev",
  "private": true,
  "description": "Local development scripts (not published to npm)",
  "scripts": {
    "build:pkg": "wasm-pack build --target web --release --out-dir pkg",
    "demo": "npm run build:pkg && npx --yes http-server . -p 8080 -c-1",
    "test:wasm": "wasm-pack test --node --release -- --test web",
    "check": "cargo fmt --all -- --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test"
  }
}
