# wasm-spatial-core (docs copy)

This file is a **stale mirror** used by older doc tooling. For accurate npm delivery,
feature flags, and API examples, use the repository root:

- **[README.md](../../README.md)** — product overview and npm vs custom-build matrix
- **[npm/README.md](../../npm/README.md)** — npm package–focused reference
- **[Live API docs](https://reed-soul.github.io/wasm-spatial-core/docs/)** — generated from WASM bindings

**Default npm WASM features:** `point-cloud` + `geotiff` (LAS/PLY/OBJ + GeoTIFF terrain).
LAZ/COPC, E57, `terrain-edit`, and `mesh-edit` require custom `wasm-pack` builds.
